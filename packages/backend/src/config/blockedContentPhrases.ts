/** Phrases seen in known consent-wall / ad-block-nag / bot-block interstitials, keyed by nothing in
 *  particular — any match anywhere in scraped text means the page isn't the real article. */
export const BLOCKED_CONTENT_PHRASES: string[] = ['Neblokujete reklamy', 'Chci čtení bez reklam']
